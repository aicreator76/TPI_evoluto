name: CI
on:
  pull_request:
    branches: [ main, feat/ci-enterprise ]
  push:
    branches: [ feat/ci-enterprise ]
jobs:
  lint-type:
    name: Lint & Typecheck (${{ matrix.py }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        py: ["3.10","3.11"]
    steps:
      - run: echo "Legacy CI sostituita: controlli reali su 'CI – Enterprise'"; exit 0
  tests:
    name: Tests & Coverage
    runs-on: ubuntu-latest
    steps:
      - run: echo "Shim OK"; exit 0
